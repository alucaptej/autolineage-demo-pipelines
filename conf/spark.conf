# Spark properties for all pipeline jobs (spark-submit --properties-file).
# Secrets (DataHub token) are injected by run.sh from the environment — never here.

spark.master                                local[1]
spark.driver.memory                         1g
spark.ui.enabled                            false

# Delta Lake
spark.sql.extensions                        io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog             org.apache.spark.sql.delta.catalog.DeltaCatalog

# DataHub lineage emission.
# spark.datahub.rest.server must point at the live DataHub GMS. A dead endpoint
# makes DatahubSparkListener drop every emission silently — the Spark job still
# succeeds and CI stays green while the lineage graph goes stale
# (incident urn:li:incident:3743cca0-0314-4215-b2bf-53990550ec89).
spark.extraListeners                        datahub.spark.DatahubSparkListener
spark.datahub.rest.server                   http://localhost:8080
spark.datahub.metadata.dataset.materialize  true
